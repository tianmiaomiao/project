<!-- 消息提示组件totast -->
```
一、 倒入插件 import Toast from 'index.js'

二、默认调用
    Toast('默认居中');


    参数配置调用
    Toast({
        message : "位置设定和时间设定",
        position: 'top',
        duration : 1000    ```显示时长
    })
```
